#!/usr/bin/env node
"use strict";
var program = require('commander');

program
  .version(require('../package.json').version)
  .command('sync <master> <target>', 'syncs <master> locale with <target> locale')
  .parse(process.argv);
